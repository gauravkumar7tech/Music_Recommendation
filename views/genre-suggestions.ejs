<!DOCTYPE html>
<html>
<head>
    <title><%= genre %> Music - Music Discovery Hub</title>
    <link rel="stylesheet" href="/style.css">
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css" rel="stylesheet">
</head>
<body>
    <header class="header">
        <div class="container">
            <h1><i class="fas fa-music"></i> Music Discovery Hub</h1>
            <nav class="nav">
                <a href="/" class="nav-link"><i class="fas fa-home"></i> Home</a>
                <a href="/add" class="nav-link"><i class="fas fa-plus"></i> Add Music</a>
                <a href="/random" class="nav-link"><i class="fas fa-random"></i> Surprise Me</a>
            </nav>
        </div>
    </header>

    <main class="main">
        <div class="container">
            <div class="genre-header">
                <h2><i class="fas fa-headphones"></i> <%= genre %> Music Collection</h2>
                <p>Discover the best <%= genre.toLowerCase() %> tracks in our library</p>
                <a href="/" class="back-btn">
                    <i class="fas fa-arrow-left"></i> Back to All Music
                </a>
            </div>

            <% if (music && music.length > 0) { %>
                <div class="music-grid">
                    <% music.forEach(song => { %>
                        <div class="music-card featured">
                            <div class="music-header">
                                <h4><%= song.title %></h4>
                                <div class="rating">
                                    <% for(let i = 1; i <= 5; i++) { %>
                                        <i class="fas fa-star <%= song.rating >= i*2 ? 'filled' : '' %>"></i>
                                    <% } %>
                                    <span><%= song.rating %>/10</span>
                                </div>
                            </div>
                            <div class="music-info">
                                <p><i class="fas fa-user"></i> <strong><%= song.artist %></strong></p>
                                <p><i class="fas fa-album"></i> <%= song.album %></p>
                                <p><i class="fas fa-calendar"></i> <%= song.year %></p>
                                <p><i class="fas fa-clock"></i> <%= song.duration %></p>
                            </div>
                            <p class="description"><%= song.description %></p>
                        </div>
                    <% }) %>
                </div>
            <% } else { %>
                <div class="no-music">
                    <i class="fas fa-music-slash"></i>
                    <h3>No <%= genre %> music found</h3>
                    <p>Be the first to add some <%= genre.toLowerCase() %> tracks!</p>
                    <a href="/add" class="btn-primary">
                        <i class="fas fa-plus"></i> Add <%= genre %> Music
                    </a>
                </div>
            <% } %>
        </div>
    </main>

    <footer class="footer">
        <div class="container">
            <p>&copy; 2024 Music Discovery Hub. Find your rhythm.</p>
        </div>
    </footer>
</body>
</html>